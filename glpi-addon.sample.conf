[logging]
# Logging level: change to DEBUG for more information, INFO for less information
level = DEBUG
# level = INFO

[GLPI]
# URL of GLPI server
url = http://glpi


[idmefv2]
# list of message processor classes
processors = NullProcessor, ReverseDNSProcessor
